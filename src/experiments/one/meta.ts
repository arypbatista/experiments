import type { ExperimentMeta } from '../types';

const meta: ExperimentMeta = {
  title: 'One',
  description: 'The Creation of Adam rendered as a field of white dots using a WebGL fragment shader.',
  tags: ['webgl', 'shader', 'dots'],
  date: '2026-04-07',
};

export default meta;
